package com.newscorp.sampleapp.network.utils

class NetworkConstants {
    companion object {
        // TODO This can be moved to build.gradle and make it different for different build versions
        const val BASE_URL = "https://newsapi.org/v2/"
        const val API_COUNTRY_DEFAULT = "au"
        const val PREF_KEY_MOCK_MODE = "PREF_KEY_MOCK_MODE"
    }
}